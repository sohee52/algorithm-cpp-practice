#include <bits/stdc++.h>
using namespace std;
string s;

void zero(string s) {
    int i  = 1;
    while (true) {
        s[i]
    }
}

void parse(string s) {
    if (s[0] == '0') {
        
    } 
}

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	cin >> s;

    parse(s);
}
